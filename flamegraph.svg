<?xml version="1.0" standalone="no"?><!DOCTYPE svg PUBLIC "-//W3C//DTD SVG 1.1//EN" "http://www.w3.org/Graphics/SVG/1.1/DTD/svg11.dtd"><svg version="1.1" width="1200" height="454" onload="init(evt)" viewBox="0 0 1200 454" xmlns="http://www.w3.org/2000/svg" xmlns:xlink="http://www.w3.org/1999/xlink" xmlns:fg="http://github.com/jonhoo/inferno"><!--Flame graph stack visualization. See https://github.com/brendangregg/FlameGraph for latest version, and http://www.brendangregg.com/flamegraphs.html for examples.--><!--NOTES: --><defs><linearGradient id="background" y1="0" y2="1" x1="0" x2="0"><stop stop-color="#eeeeee" offset="5%"/><stop stop-color="#eeeeb0" offset="95%"/></linearGradient></defs><style type="text/css">
text { font-family:monospace; font-size:12px }
#title { text-anchor:middle; font-size:17px; }
#matched { text-anchor:end; }
#search { text-anchor:end; opacity:0.1; cursor:pointer; }
#search:hover, #search.show { opacity:1; }
#subtitle { text-anchor:middle; font-color:rgb(160,160,160); }
#unzoom { cursor:pointer; }
#frames > *:hover { stroke:black; stroke-width:0.5; cursor:pointer; }
.hide { display:none; }
.parent { opacity:0.5; }
</style><script type="text/ecmascript"><![CDATA[
        var nametype = 'Function:';
        var fontsize = 12;
        var fontwidth = 0.59;
        var xpad = 10;
        var inverted = false;
        var searchcolor = 'rgb(230,0,230)';
        var fluiddrawing = true;
        var truncate_text_right = false;
    ]]><![CDATA["use strict";
var details, searchbtn, unzoombtn, matchedtxt, svg, searching, frames, known_font_width;
function init(evt) {
    details = document.getElementById("details").firstChild;
    searchbtn = document.getElementById("search");
    unzoombtn = document.getElementById("unzoom");
    matchedtxt = document.getElementById("matched");
    svg = document.getElementsByTagName("svg")[0];
    frames = document.getElementById("frames");
    known_font_width = get_monospace_width(frames);
    total_samples = parseInt(frames.attributes.total_samples.value);
    searching = 0;

    // Use GET parameters to restore a flamegraph's state.
    var restore_state = function() {
        var params = get_params();
        if (params.x && params.y)
            zoom(find_group(document.querySelector('[*|x="' + params.x + '"][y="' + params.y + '"]')));
        if (params.s)
            search(params.s);
    };

    if (fluiddrawing) {
        // Make width dynamic so the SVG fits its parent's width.
        svg.removeAttribute("width");
        // Edge requires us to have a viewBox that gets updated with size changes.
        var isEdge = /Edge\/\d./i.test(navigator.userAgent);
        if (!isEdge) {
            svg.removeAttribute("viewBox");
        }
        var update_for_width_change = function() {
            if (isEdge) {
                svg.attributes.viewBox.value = "0 0 " + svg.width.baseVal.value + " " + svg.height.baseVal.value;
            }

            // Keep consistent padding on left and right of frames container.
            frames.attributes.width.value = svg.width.baseVal.value - xpad * 2;

            // Text truncation needs to be adjusted for the current width.
            update_text_for_elements(frames.children);

            // Keep search elements at a fixed distance from right edge.
            var svgWidth = svg.width.baseVal.value;
            searchbtn.attributes.x.value = svgWidth - xpad;
            matchedtxt.attributes.x.value = svgWidth - xpad;
        };
        window.addEventListener('resize', function() {
            update_for_width_change();
        });
        // This needs to be done asynchronously for Safari to work.
        setTimeout(function() {
            unzoom();
            update_for_width_change();
            restore_state();
        }, 0);
    } else {
        restore_state();
    }
}
// event listeners
window.addEventListener("click", function(e) {
    var target = find_group(e.target);
    if (target) {
        if (target.nodeName == "a") {
            if (e.ctrlKey === false) return;
            e.preventDefault();
        }
        if (target.classList.contains("parent")) unzoom();
        zoom(target);

        // set parameters for zoom state
        var el = target.querySelector("rect");
        if (el && el.attributes && el.attributes.y && el.attributes["fg:x"]) {
            var params = get_params()
            params.x = el.attributes["fg:x"].value;
            params.y = el.attributes.y.value;
            history.replaceState(null, null, parse_params(params));
        }
    }
    else if (e.target.id == "unzoom") {
        unzoom();

        // remove zoom state
        var params = get_params();
        if (params.x) delete params.x;
        if (params.y) delete params.y;
        history.replaceState(null, null, parse_params(params));
    }
    else if (e.target.id == "search") search_prompt();
}, false)
// mouse-over for info
// show
window.addEventListener("mouseover", function(e) {
    var target = find_group(e.target);
    if (target) details.nodeValue = nametype + " " + g_to_text(target);
}, false)
// clear
window.addEventListener("mouseout", function(e) {
    var target = find_group(e.target);
    if (target) details.nodeValue = ' ';
}, false)
// ctrl-F for search
window.addEventListener("keydown",function (e) {
    if (e.keyCode === 114 || (e.ctrlKey && e.keyCode === 70)) {
        e.preventDefault();
        search_prompt();
    }
}, false)
// functions
function get_params() {
    var params = {};
    var paramsarr = window.location.search.substr(1).split('&');
    for (var i = 0; i < paramsarr.length; ++i) {
        var tmp = paramsarr[i].split("=");
        if (!tmp[0] || !tmp[1]) continue;
        params[tmp[0]]  = decodeURIComponent(tmp[1]);
    }
    return params;
}
function parse_params(params) {
    var uri = "?";
    for (var key in params) {
        uri += key + '=' + encodeURIComponent(params[key]) + '&';
    }
    if (uri.slice(-1) == "&")
        uri = uri.substring(0, uri.length - 1);
    if (uri == '?')
        uri = window.location.href.split('?')[0];
    return uri;
}
function find_child(node, selector) {
    var children = node.querySelectorAll(selector);
    if (children.length) return children[0];
    return;
}
function find_group(node) {
    var parent = node.parentElement;
    if (!parent) return;
    if (parent.id == "frames") return node;
    return find_group(parent);
}
function orig_save(e, attr, val) {
    if (e.attributes["fg:orig_" + attr] != undefined) return;
    if (e.attributes[attr] == undefined) return;
    if (val == undefined) val = e.attributes[attr].value;
    e.setAttribute("fg:orig_" + attr, val);
}
function orig_load(e, attr) {
    if (e.attributes["fg:orig_"+attr] == undefined) return;
    e.attributes[attr].value = e.attributes["fg:orig_" + attr].value;
    e.removeAttribute("fg:orig_" + attr);
}
function g_to_text(e) {
    var text = find_child(e, "title").firstChild.nodeValue;
    return (text)
}
function g_to_func(e) {
    var func = g_to_text(e);
    // if there's any manipulation we want to do to the function
    // name before it's searched, do it here before returning.
    return (func);
}
function get_monospace_width(frames) {
    // Given the id="frames" element, return the width of text characters if
    // this is a monospace font, otherwise return 0.
    text = find_child(frames.children[0], "text");
    originalContent = text.textContent;
    text.textContent = "!";
    bangWidth = text.getComputedTextLength();
    text.textContent = "W";
    wWidth = text.getComputedTextLength();
    text.textContent = originalContent;
    if (bangWidth === wWidth) {
        return bangWidth;
    } else {
        return 0;
    }
}
function update_text_for_elements(elements) {
    // In order to render quickly in the browser, you want to do one pass of
    // reading attributes, and one pass of mutating attributes. See
    // https://web.dev/avoid-large-complex-layouts-and-layout-thrashing/ for details.

    // Fall back to inefficient calculation, if we're variable-width font.
    // TODO This should be optimized somehow too.
    if (known_font_width === 0) {
        for (var i = 0; i < elements.length; i++) {
            update_text(elements[i]);
        }
        return;
    }

    var textElemNewAttributes = [];
    for (var i = 0; i < elements.length; i++) {
        var e = elements[i];
        var r = find_child(e, "rect");
        var t = find_child(e, "text");
        var w = parseFloat(r.attributes.width.value) * frames.attributes.width.value / 100 - 3;
        var txt = find_child(e, "title").textContent.replace(/\([^(]*\)$/,"");
        var newX = format_percent((parseFloat(r.attributes.x.value) + (100 * 3 / frames.attributes.width.value)));

        // Smaller than this size won't fit anything
        if (w < 2 * known_font_width) {
            textElemNewAttributes.push([newX, ""]);
            continue;
        }

        // Fit in full text width
        if (txt.length * known_font_width < w) {
            textElemNewAttributes.push([newX, txt]);
            continue;
        }

        var substringLength = Math.floor(w / known_font_width) - 2;
        if (truncate_text_right) {
            // Truncate the right side of the text.
            textElemNewAttributes.push([newX, txt.substring(0, substringLength) + ".."]);
            continue;
        } else {
            // Truncate the left side of the text.
            textElemNewAttributes.push([newX, ".." + txt.substring(txt.length - substringLength, txt.length)]);
            continue;
        }
    }

    console.assert(textElemNewAttributes.length === elements.length, "Resize failed, please file a bug at https://github.com/jonhoo/inferno/");

    // Now that we know new textContent, set it all in one go so we don't refresh a bazillion times.
    for (var i = 0; i < elements.length; i++) {
        var e = elements[i];
        var values = textElemNewAttributes[i];
        var t = find_child(e, "text");
        t.attributes.x.value = values[0];
        t.textContent = values[1];
    }
}

function update_text(e) {
    var r = find_child(e, "rect");
    var t = find_child(e, "text");
    var w = parseFloat(r.attributes.width.value) * frames.attributes.width.value / 100 - 3;
    var txt = find_child(e, "title").textContent.replace(/\([^(]*\)$/,"");
    t.attributes.x.value = format_percent((parseFloat(r.attributes.x.value) + (100 * 3 / frames.attributes.width.value)));

    // Smaller than this size won't fit anything
    if (w < 2 * fontsize * fontwidth) {
        t.textContent = "";
        return;
    }
    t.textContent = txt;
    // Fit in full text width
    if (t.getComputedTextLength() < w)
        return;
    if (truncate_text_right) {
        // Truncate the right side of the text.
        for (var x = txt.length - 2; x > 0; x--) {
            if (t.getSubStringLength(0, x + 2) <= w) {
                t.textContent = txt.substring(0, x) + "..";
                return;
            }
        }
    } else {
        // Truncate the left side of the text.
        for (var x = 2; x < txt.length; x++) {
            if (t.getSubStringLength(x - 2, txt.length) <= w) {
                t.textContent = ".." + txt.substring(x, txt.length);
                return;
            }
        }
    }
    t.textContent = "";
}
// zoom
function zoom_reset(e) {
    if (e.tagName == "rect") {
        e.attributes.x.value = format_percent(100 * parseInt(e.attributes["fg:x"].value) / total_samples);
        e.attributes.width.value = format_percent(100 * parseInt(e.attributes["fg:w"].value) / total_samples);
    }
    if (e.childNodes == undefined) return;
    for(var i = 0, c = e.childNodes; i < c.length; i++) {
        zoom_reset(c[i]);
    }
}
function zoom_child(e, x, zoomed_width_samples) {
    if (e.tagName == "text") {
        var parent_x = parseFloat(find_child(e.parentNode, "rect[x]").attributes.x.value);
        e.attributes.x.value = format_percent(parent_x + (100 * 3 / frames.attributes.width.value));
    } else if (e.tagName == "rect") {
        e.attributes.x.value = format_percent(100 * (parseInt(e.attributes["fg:x"].value) - x) / zoomed_width_samples);
        e.attributes.width.value = format_percent(100 * parseInt(e.attributes["fg:w"].value) / zoomed_width_samples);
    }
    if (e.childNodes == undefined) return;
    for(var i = 0, c = e.childNodes; i < c.length; i++) {
        zoom_child(c[i], x, zoomed_width_samples);
    }
}
function zoom_parent(e) {
    if (e.attributes) {
        if (e.attributes.x != undefined) {
            e.attributes.x.value = "0.0%";
        }
        if (e.attributes.width != undefined) {
            e.attributes.width.value = "100.0%";
        }
    }
    if (e.childNodes == undefined) return;
    for(var i = 0, c = e.childNodes; i < c.length; i++) {
        zoom_parent(c[i]);
    }
}
function zoom(node) {
    var attr = find_child(node, "rect").attributes;
    var width = parseInt(attr["fg:w"].value);
    var xmin = parseInt(attr["fg:x"].value);
    var xmax = xmin + width;
    var ymin = parseFloat(attr.y.value);
    unzoombtn.classList.remove("hide");
    var el = frames.children;
    var to_update_text = [];
    for (var i = 0; i < el.length; i++) {
        var e = el[i];
        var a = find_child(e, "rect").attributes;
        var ex = parseInt(a["fg:x"].value);
        var ew = parseInt(a["fg:w"].value);
        // Is it an ancestor
        if (!inverted) {
            var upstack = parseFloat(a.y.value) > ymin;
        } else {
            var upstack = parseFloat(a.y.value) < ymin;
        }
        if (upstack) {
            // Direct ancestor
            if (ex <= xmin && (ex+ew) >= xmax) {
                e.classList.add("parent");
                zoom_parent(e);
                to_update_text.push(e);
            }
            // not in current path
            else
                e.classList.add("hide");
        }
        // Children maybe
        else {
            // no common path
            if (ex < xmin || ex >= xmax) {
                e.classList.add("hide");
            }
            else {
                zoom_child(e, xmin, width);
                to_update_text.push(e);
            }
        }
    }
    update_text_for_elements(to_update_text);
}
function unzoom() {
    unzoombtn.classList.add("hide");
    var el = frames.children;
    for(var i = 0; i < el.length; i++) {
        el[i].classList.remove("parent");
        el[i].classList.remove("hide");
        zoom_reset(el[i]);
    }
    update_text_for_elements(el);
}
// search
function reset_search() {
    var el = document.querySelectorAll("#frames rect");
    for (var i = 0; i < el.length; i++) {
        orig_load(el[i], "fill")
    }
    var params = get_params();
    delete params.s;
    history.replaceState(null, null, parse_params(params));
}
function search_prompt() {
    if (!searching) {
        var term = prompt("Enter a search term (regexp " +
            "allowed, eg: ^ext4_)", "");
        if (term != null) {
            search(term)
        }
    } else {
        reset_search();
        searching = 0;
        searchbtn.classList.remove("show");
        searchbtn.firstChild.nodeValue = "Search"
        matchedtxt.classList.add("hide");
        matchedtxt.firstChild.nodeValue = ""
    }
}
function search(term) {
    var re = new RegExp(term);
    var el = frames.children;
    var matches = new Object();
    var maxwidth = 0;
    for (var i = 0; i < el.length; i++) {
        var e = el[i];
        // Skip over frames which are either not visible, or below the zoomed-to frame
        if (e.classList.contains("hide") || e.classList.contains("parent")) {
            continue;
        }
        var func = g_to_func(e);
        var rect = find_child(e, "rect");
        if (func == null || rect == null)
            continue;
        // Save max width. Only works as we have a root frame
        var w = parseInt(rect.attributes["fg:w"].value);
        if (w > maxwidth)
            maxwidth = w;
        if (func.match(re)) {
            // highlight
            var x = parseInt(rect.attributes["fg:x"].value);
            orig_save(rect, "fill");
            rect.attributes.fill.value = searchcolor;
            // remember matches
            if (matches[x] == undefined) {
                matches[x] = w;
            } else {
                if (w > matches[x]) {
                    // overwrite with parent
                    matches[x] = w;
                }
            }
            searching = 1;
        }
    }
    if (!searching)
        return;
    var params = get_params();
    params.s = term;
    history.replaceState(null, null, parse_params(params));

    searchbtn.classList.add("show");
    searchbtn.firstChild.nodeValue = "Reset Search";
    // calculate percent matched, excluding vertical overlap
    var count = 0;
    var lastx = -1;
    var lastw = 0;
    var keys = Array();
    for (k in matches) {
        if (matches.hasOwnProperty(k))
            keys.push(k);
    }
    // sort the matched frames by their x location
    // ascending, then width descending
    keys.sort(function(a, b){
        return a - b;
    });
    // Step through frames saving only the biggest bottom-up frames
    // thanks to the sort order. This relies on the tree property
    // where children are always smaller than their parents.
    for (var k in keys) {
        var x = parseInt(keys[k]);
        var w = matches[keys[k]];
        if (x >= lastx + lastw) {
            count += w;
            lastx = x;
            lastw = w;
        }
    }
    // display matched percent
    matchedtxt.classList.remove("hide");
    var pct = 100 * count / maxwidth;
    if (pct != 100) pct = pct.toFixed(1);
    matchedtxt.firstChild.nodeValue = "Matched: " + pct + "%";
}
function format_percent(n) {
    return n.toFixed(4) + "%";
}
]]></script><rect x="0" y="0" width="100%" height="454" fill="url(#background)"/><text id="title" fill="rgb(0,0,0)" x="50.0000%" y="24.00">Flame Graph</text><text id="details" fill="rgb(0,0,0)" x="10" y="437.00"> </text><text id="unzoom" class="hide" fill="rgb(0,0,0)" x="10" y="24.00">Reset Zoom</text><text id="search" fill="rgb(0,0,0)" x="1190" y="24.00">Search</text><text id="matched" fill="rgb(0,0,0)" x="1190" y="437.00"> </text><svg id="frames" x="10" width="1180" total_samples="6653"><g><title>0x1031183d3 (1 samples, 0.02%)</title><rect x="0.0000%" y="389" width="0.0150%" height="15" fill="rgb(227,0,7)" fg:x="0" fg:w="1"/><text x="0.2500%" y="399.50"></text></g><g><title>0x10315fca7 (1 samples, 0.02%)</title><rect x="0.0000%" y="373" width="0.0150%" height="15" fill="rgb(217,0,24)" fg:x="0" fg:w="1"/><text x="0.2500%" y="383.50"></text></g><g><title>0x1031797eb (1 samples, 0.02%)</title><rect x="0.0000%" y="357" width="0.0150%" height="15" fill="rgb(221,193,54)" fg:x="0" fg:w="1"/><text x="0.2500%" y="367.50"></text></g><g><title>0x103179fdf (1 samples, 0.02%)</title><rect x="0.0000%" y="341" width="0.0150%" height="15" fill="rgb(248,212,6)" fg:x="0" fg:w="1"/><text x="0.2500%" y="351.50"></text></g><g><title>0x10311b581 (1 samples, 0.02%)</title><rect x="0.0000%" y="325" width="0.0150%" height="15" fill="rgb(208,68,35)" fg:x="0" fg:w="1"/><text x="0.2500%" y="335.50"></text></g><g><title>dyld4::APIs::runAllInitializersForMain() (1 samples, 0.02%)</title><rect x="0.0150%" y="357" width="0.0150%" height="15" fill="rgb(232,128,0)" fg:x="1" fg:w="1"/><text x="0.2650%" y="367.50"></text></g><g><title>dyld4::PrebuiltLoader::runInitializers(dyld4::RuntimeState&amp;) const (1 samples, 0.02%)</title><rect x="0.0150%" y="341" width="0.0150%" height="15" fill="rgb(207,160,47)" fg:x="1" fg:w="1"/><text x="0.2650%" y="351.50"></text></g><g><title>dyld4::Loader::findAndRunAllInitializers(dyld4::RuntimeState&amp;) const (1 samples, 0.02%)</title><rect x="0.0150%" y="325" width="0.0150%" height="15" fill="rgb(228,23,34)" fg:x="1" fg:w="1"/><text x="0.2650%" y="335.50"></text></g><g><title>dyld3::MachOAnalyzer::forEachInitializer(Diagnostics&amp;, dyld3::MachOAnalyzer::VMAddrConverter const&amp;, void (unsigned int) block_pointer, void const*) const (1 samples, 0.02%)</title><rect x="0.0150%" y="309" width="0.0150%" height="15" fill="rgb(218,30,26)" fg:x="1" fg:w="1"/><text x="0.2650%" y="319.50"></text></g><g><title>mach_o::Header::forEachSection(void (mach_o::Header::SectionInfo const&amp;, bool&amp;) block_pointer) const (1 samples, 0.02%)</title><rect x="0.0150%" y="293" width="0.0150%" height="15" fill="rgb(220,122,19)" fg:x="1" fg:w="1"/><text x="0.2650%" y="303.50"></text></g><g><title>mach_o::Header::forEachLoadCommand(void (load_command const*, bool&amp;) block_pointer) const (1 samples, 0.02%)</title><rect x="0.0150%" y="277" width="0.0150%" height="15" fill="rgb(250,228,42)" fg:x="1" fg:w="1"/><text x="0.2650%" y="287.50"></text></g><g><title>invocation function for block in mach_o::Header::forEachSection(void (mach_o::Header::SectionInfo const&amp;, bool&amp;) block_pointer) const (1 samples, 0.02%)</title><rect x="0.0150%" y="261" width="0.0150%" height="15" fill="rgb(240,193,28)" fg:x="1" fg:w="1"/><text x="0.2650%" y="271.50"></text></g><g><title>invocation function for block in dyld3::MachOAnalyzer::forEachInitializer(Diagnostics&amp;, dyld3::MachOAnalyzer::VMAddrConverter const&amp;, void (unsigned int) block_pointer, void const*) const (1 samples, 0.02%)</title><rect x="0.0150%" y="245" width="0.0150%" height="15" fill="rgb(216,20,37)" fg:x="1" fg:w="1"/><text x="0.2650%" y="255.50"></text></g><g><title>invocation function for block in dyld4::Loader::findAndRunAllInitializers(dyld4::RuntimeState&amp;) const (1 samples, 0.02%)</title><rect x="0.0150%" y="229" width="0.0150%" height="15" fill="rgb(206,188,39)" fg:x="1" fg:w="1"/><text x="0.2650%" y="239.50"></text></g><g><title>libSystem_initializer (1 samples, 0.02%)</title><rect x="0.0150%" y="213" width="0.0150%" height="15" fill="rgb(217,207,13)" fg:x="1" fg:w="1"/><text x="0.2650%" y="223.50"></text></g><g><title>_libxpc_initializer (1 samples, 0.02%)</title><rect x="0.0150%" y="197" width="0.0150%" height="15" fill="rgb(231,73,38)" fg:x="1" fg:w="1"/><text x="0.2650%" y="207.50"></text></g><g><title>_xpc_collect_images (1 samples, 0.02%)</title><rect x="0.0150%" y="181" width="0.0150%" height="15" fill="rgb(225,20,46)" fg:x="1" fg:w="1"/><text x="0.2650%" y="191.50"></text></g><g><title>xpc_bundle_copy_services (1 samples, 0.02%)</title><rect x="0.0150%" y="165" width="0.0150%" height="15" fill="rgb(210,31,41)" fg:x="1" fg:w="1"/><text x="0.2650%" y="175.50"></text></g><g><title>_xpc_bundle_resolve (1 samples, 0.02%)</title><rect x="0.0150%" y="149" width="0.0150%" height="15" fill="rgb(221,200,47)" fg:x="1" fg:w="1"/><text x="0.2650%" y="159.50"></text></g><g><title>_dispatch_once_callout (1 samples, 0.02%)</title><rect x="0.0150%" y="133" width="0.0150%" height="15" fill="rgb(226,26,5)" fg:x="1" fg:w="1"/><text x="0.2650%" y="143.50"></text></g><g><title>_dispatch_client_callout (1 samples, 0.02%)</title><rect x="0.0150%" y="117" width="0.0150%" height="15" fill="rgb(249,33,26)" fg:x="1" fg:w="1"/><text x="0.2650%" y="127.50"></text></g><g><title>___xpc_bundle_resolve_block_invoke (1 samples, 0.02%)</title><rect x="0.0150%" y="101" width="0.0150%" height="15" fill="rgb(235,183,28)" fg:x="1" fg:w="1"/><text x="0.2650%" y="111.50"></text></g><g><title>_xpc_bundle_resolve_sync (1 samples, 0.02%)</title><rect x="0.0150%" y="85" width="0.0150%" height="15" fill="rgb(221,5,38)" fg:x="1" fg:w="1"/><text x="0.2650%" y="95.50"></text></g><g><title>_xpc_bundle_resolve_root (1 samples, 0.02%)</title><rect x="0.0150%" y="69" width="0.0150%" height="15" fill="rgb(247,18,42)" fg:x="1" fg:w="1"/><text x="0.2650%" y="79.50"></text></g><g><title>_resolve_and_stat_path (1 samples, 0.02%)</title><rect x="0.0150%" y="53" width="0.0150%" height="15" fill="rgb(241,131,45)" fg:x="1" fg:w="1"/><text x="0.2650%" y="63.50"></text></g><g><title>__mac_syscall (1 samples, 0.02%)</title><rect x="0.0150%" y="37" width="0.0150%" height="15" fill="rgb(249,31,29)" fg:x="1" fg:w="1"/><text x="0.2650%" y="47.50"></text></g><g><title>dyld4::prepare(dyld4::APIs&amp;, mach_o::Header const*) (2 samples, 0.03%)</title><rect x="0.0150%" y="373" width="0.0301%" height="15" fill="rgb(225,111,53)" fg:x="1" fg:w="2"/><text x="0.2650%" y="383.50"></text></g><g><title>dyld4::RuntimeState::notifyDtrace(std::__1::span&lt;dyld4::Loader const*, 18446744073709551615ul&gt; const&amp;) (1 samples, 0.02%)</title><rect x="0.0301%" y="357" width="0.0150%" height="15" fill="rgb(238,160,17)" fg:x="2" fg:w="1"/><text x="0.2801%" y="367.50"></text></g><g><title>dyld4::SyscallDelegate::dtraceRegisterUserProbes(dof_ioctl_data*) const (1 samples, 0.02%)</title><rect x="0.0301%" y="341" width="0.0150%" height="15" fill="rgb(214,148,48)" fg:x="2" fg:w="1"/><text x="0.2801%" y="351.50"></text></g><g><title>open (1 samples, 0.02%)</title><rect x="0.0301%" y="325" width="0.0150%" height="15" fill="rgb(232,36,49)" fg:x="2" fg:w="1"/><text x="0.2801%" y="335.50"></text></g><g><title>__open (1 samples, 0.02%)</title><rect x="0.0301%" y="309" width="0.0150%" height="15" fill="rgb(209,103,24)" fg:x="2" fg:w="1"/><text x="0.2801%" y="319.50"></text></g><g><title>__bzero (180 samples, 2.71%)</title><rect x="0.0451%" y="341" width="2.7055%" height="15" fill="rgb(229,88,8)" fg:x="3" fg:w="180"/><text x="0.2951%" y="351.50">__..</text></g><g><title>_free (12 samples, 0.18%)</title><rect x="2.7506%" y="341" width="0.1804%" height="15" fill="rgb(213,181,19)" fg:x="183" fg:w="12"/><text x="3.0006%" y="351.50"></text></g><g><title>_malloc_zone_calloc (2 samples, 0.03%)</title><rect x="2.9310%" y="341" width="0.0301%" height="15" fill="rgb(254,191,54)" fg:x="195" fg:w="2"/><text x="3.1810%" y="351.50"></text></g><g><title>_xzm_free (49 samples, 0.74%)</title><rect x="2.9611%" y="341" width="0.7365%" height="15" fill="rgb(241,83,37)" fg:x="197" fg:w="49"/><text x="3.2111%" y="351.50"></text></g><g><title>_xzm_malloc_zone_malloc_type_calloc_entry (1 samples, 0.02%)</title><rect x="3.6976%" y="341" width="0.0150%" height="15" fill="rgb(233,36,39)" fg:x="246" fg:w="1"/><text x="3.9476%" y="351.50"></text></g><g><title>_xzm_xzone_malloc (1 samples, 0.02%)</title><rect x="3.7126%" y="341" width="0.0150%" height="15" fill="rgb(226,3,54)" fg:x="247" fg:w="1"/><text x="3.9626%" y="351.50"></text></g><g><title>_xzm_xzone_malloc_tiny (7 samples, 0.11%)</title><rect x="3.7276%" y="341" width="0.1052%" height="15" fill="rgb(245,192,40)" fg:x="248" fg:w="7"/><text x="3.9776%" y="351.50"></text></g><g><title>&lt;core::ops::range::Range&lt;T&gt; as core::iter::range::RangeIteratorImpl&gt;::spec_next (12 samples, 0.18%)</title><rect x="3.8329%" y="325" width="0.1804%" height="15" fill="rgb(238,167,29)" fg:x="255" fg:w="12"/><text x="4.0829%" y="335.50"></text></g><g><title>&lt;usize as core::slice::index::SliceIndex&lt;[T]&gt;&gt;::index (73 samples, 1.10%)</title><rect x="4.0132%" y="325" width="1.0972%" height="15" fill="rgb(232,182,51)" fg:x="267" fg:w="73"/><text x="4.2632%" y="335.50"></text></g><g><title>&lt;usize as core::slice::index::SliceIndex&lt;[T]&gt;&gt;::index_mut (15 samples, 0.23%)</title><rect x="5.1105%" y="325" width="0.2255%" height="15" fill="rgb(231,60,39)" fg:x="340" fg:w="15"/><text x="5.3605%" y="335.50"></text></g><g><title>PolyQ::sim::amplitude_clifford_t_accel (1,350 samples, 20.29%)</title><rect x="5.3359%" y="325" width="20.2916%" height="15" fill="rgb(208,69,12)" fg:x="355" fg:w="1350"/><text x="5.5859%" y="335.50">PolyQ::sim::amplitude_clifford_t..</text></g><g><title>_malloc_zone_calloc (86 samples, 1.29%)</title><rect x="25.6275%" y="325" width="1.2926%" height="15" fill="rgb(235,93,37)" fg:x="1705" fg:w="86"/><text x="25.8775%" y="335.50"></text></g><g><title>_xzm_malloc_zone_malloc_type_calloc_entry (86 samples, 1.29%)</title><rect x="26.9202%" y="325" width="1.2926%" height="15" fill="rgb(213,116,39)" fg:x="1791" fg:w="86"/><text x="27.1702%" y="335.50"></text></g><g><title>_xzm_xzone_malloc (64 samples, 0.96%)</title><rect x="28.2128%" y="325" width="0.9620%" height="15" fill="rgb(222,207,29)" fg:x="1877" fg:w="64"/><text x="28.4628%" y="335.50"></text></g><g><title>_xzm_xzone_malloc_tiny (232 samples, 3.49%)</title><rect x="29.1748%" y="325" width="3.4871%" height="15" fill="rgb(206,96,30)" fg:x="1941" fg:w="232"/><text x="29.4248%" y="335.50">_xz..</text></g><g><title>alloc::raw_vec::RawVecInner&lt;A&gt;::current_memory (1 samples, 0.02%)</title><rect x="32.6620%" y="325" width="0.0150%" height="15" fill="rgb(218,138,4)" fg:x="2173" fg:w="1"/><text x="32.9120%" y="335.50"></text></g><g><title>alloc::raw_vec::RawVecInner&lt;A&gt;::deallocate (45 samples, 0.68%)</title><rect x="32.6770%" y="325" width="0.6764%" height="15" fill="rgb(250,191,14)" fg:x="2174" fg:w="45"/><text x="32.9270%" y="335.50"></text></g><g><title>calloc (2 samples, 0.03%)</title><rect x="33.3534%" y="325" width="0.0301%" height="15" fill="rgb(239,60,40)" fg:x="2219" fg:w="2"/><text x="33.6034%" y="335.50"></text></g><g><title>alloc::alloc::alloc_zeroed (2,344 samples, 35.23%)</title><rect x="3.8329%" y="341" width="35.2322%" height="15" fill="rgb(206,27,48)" fg:x="255" fg:w="2344"/><text x="4.0829%" y="351.50">alloc::alloc::alloc_zeroed</text></g><g><title>core::cmp::impls::_&lt;impl core::cmp::PartialOrd for usize&gt;::lt (378 samples, 5.68%)</title><rect x="33.3834%" y="325" width="5.6816%" height="15" fill="rgb(225,35,8)" fg:x="2221" fg:w="378"/><text x="33.6334%" y="335.50">core::c..</text></g><g><title>DYLD-STUB$$_platform_bzero (3 samples, 0.05%)</title><rect x="39.0651%" y="325" width="0.0451%" height="15" fill="rgb(250,213,24)" fg:x="2599" fg:w="3"/><text x="39.3151%" y="335.50"></text></g><g><title>DYLD-STUB$$free (49 samples, 0.74%)</title><rect x="39.1102%" y="325" width="0.7365%" height="15" fill="rgb(247,123,22)" fg:x="2602" fg:w="49"/><text x="39.3602%" y="335.50"></text></g><g><title>DYLD-STUB$$mach_absolute_time (4 samples, 0.06%)</title><rect x="39.8467%" y="325" width="0.0601%" height="15" fill="rgb(231,138,38)" fg:x="2651" fg:w="4"/><text x="40.0967%" y="335.50"></text></g><g><title>PolyQ::sim::amplitude_clifford_t_accel (24 samples, 0.36%)</title><rect x="39.9068%" y="325" width="0.3607%" height="15" fill="rgb(231,145,46)" fg:x="2655" fg:w="24"/><text x="40.1568%" y="335.50"></text></g><g><title>PolyQ::sim::simulate_statevector (307 samples, 4.61%)</title><rect x="40.2675%" y="325" width="4.6145%" height="15" fill="rgb(251,118,11)" fg:x="2679" fg:w="307"/><text x="40.5175%" y="335.50">PolyQ..</text></g><g><title>_free (316 samples, 4.75%)</title><rect x="44.8820%" y="325" width="4.7497%" height="15" fill="rgb(217,147,25)" fg:x="2986" fg:w="316"/><text x="45.1320%" y="335.50">_free</text></g><g><title>_platform_memset (1 samples, 0.02%)</title><rect x="49.6317%" y="325" width="0.0150%" height="15" fill="rgb(247,81,37)" fg:x="3302" fg:w="1"/><text x="49.8817%" y="335.50"></text></g><g><title>DYLD-STUB$$_platform_bzero (39 samples, 0.59%)</title><rect x="60.9950%" y="309" width="0.5862%" height="15" fill="rgb(209,12,38)" fg:x="4058" fg:w="39"/><text x="61.2450%" y="319.50"></text></g><g><title>DYLD-STUB$$mach_absolute_time (56 samples, 0.84%)</title><rect x="61.5812%" y="309" width="0.8417%" height="15" fill="rgb(227,1,9)" fg:x="4097" fg:w="56"/><text x="61.8312%" y="319.50"></text></g><g><title>__bzero (40 samples, 0.60%)</title><rect x="62.4230%" y="309" width="0.6012%" height="15" fill="rgb(248,47,43)" fg:x="4153" fg:w="40"/><text x="62.6730%" y="319.50"></text></g><g><title>_platform_memset (13 samples, 0.20%)</title><rect x="63.0242%" y="309" width="0.1954%" height="15" fill="rgb(221,10,30)" fg:x="4193" fg:w="13"/><text x="63.2742%" y="319.50"></text></g><g><title>_xzm_free (2,810 samples, 42.24%)</title><rect x="49.6468%" y="325" width="42.2366%" height="15" fill="rgb(210,229,1)" fg:x="3303" fg:w="2810"/><text x="49.8968%" y="335.50">_xzm_free</text></g><g><title>mach_absolute_time (1,907 samples, 28.66%)</title><rect x="63.2196%" y="309" width="28.6638%" height="15" fill="rgb(222,148,37)" fg:x="4206" fg:w="1907"/><text x="63.4696%" y="319.50">mach_absolute_time</text></g><g><title>core::num::_&lt;impl usize&gt;::overflowing_mul (14 samples, 0.21%)</title><rect x="91.8834%" y="325" width="0.2104%" height="15" fill="rgb(234,67,33)" fg:x="6113" fg:w="14"/><text x="92.1334%" y="335.50"></text></g><g><title>alloc::alloc::dealloc (3,629 samples, 54.55%)</title><rect x="39.0651%" y="341" width="54.5468%" height="15" fill="rgb(247,98,35)" fg:x="2599" fg:w="3629"/><text x="39.3151%" y="351.50">alloc::alloc::dealloc</text></g><g><title>mach_absolute_time (101 samples, 1.52%)</title><rect x="92.0938%" y="325" width="1.5181%" height="15" fill="rgb(247,138,52)" fg:x="6127" fg:w="101"/><text x="92.3438%" y="335.50"></text></g><g><title>&lt;core::slice::iter::Iter&lt;T&gt; as core::iter::traits::iterator::Iterator&gt;::fold (336 samples, 5.05%)</title><rect x="93.6119%" y="325" width="5.0504%" height="15" fill="rgb(213,79,30)" fg:x="6228" fg:w="336"/><text x="93.8619%" y="335.50">&lt;core:..</text></g><g><title>&lt;f64 as core::iter::traits::accum::Sum&gt;::sum::_{{closure}} (73 samples, 1.10%)</title><rect x="98.6623%" y="325" width="1.0972%" height="15" fill="rgb(246,177,23)" fg:x="6564" fg:w="73"/><text x="98.9123%" y="335.50"></text></g><g><title>&lt;f64 as core::ops::arith::Add&gt;::add (15 samples, 0.23%)</title><rect x="99.7595%" y="325" width="0.2255%" height="15" fill="rgb(230,62,27)" fg:x="6637" fg:w="15"/><text x="100.0095%" y="335.50"></text></g><g><title>all (6,653 samples, 100%)</title><rect x="0.0000%" y="405" width="100.0000%" height="15" fill="rgb(216,154,8)" fg:x="0" fg:w="6653"/><text x="0.2500%" y="415.50"></text></g><g><title>start (6,652 samples, 99.98%)</title><rect x="0.0150%" y="389" width="99.9850%" height="15" fill="rgb(244,35,45)" fg:x="1" fg:w="6652"/><text x="0.2650%" y="399.50">start</text></g><g><title>main (6,650 samples, 99.95%)</title><rect x="0.0451%" y="373" width="99.9549%" height="15" fill="rgb(251,115,12)" fg:x="3" fg:w="6650"/><text x="0.2951%" y="383.50">main</text></g><g><title>core::ops::function::FnOnce::call_once (6,650 samples, 99.95%)</title><rect x="0.0451%" y="357" width="99.9549%" height="15" fill="rgb(240,54,50)" fg:x="3" fg:w="6650"/><text x="0.2951%" y="367.50">core::ops::function::FnOnce::call_once</text></g><g><title>benchmark::simulate_qasm_statevector (425 samples, 6.39%)</title><rect x="93.6119%" y="341" width="6.3881%" height="15" fill="rgb(233,84,52)" fg:x="6228" fg:w="425"/><text x="93.8619%" y="351.50">benchmar..</text></g><g><title>core::ptr::non_null::NonNull&lt;T&gt;::add (1 samples, 0.02%)</title><rect x="99.9850%" y="325" width="0.0150%" height="15" fill="rgb(207,117,47)" fg:x="6652" fg:w="1"/><text x="100.2350%" y="335.50"></text></g></svg></svg>